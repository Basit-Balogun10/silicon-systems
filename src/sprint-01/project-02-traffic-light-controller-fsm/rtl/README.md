# RTL Scaffold

Synthesizable SystemVerilog for the traffic light controller will live here.

This folder is intentionally empty for now. The design spec in `../docs/design-spec.md` defines the contract the RTL will need to satisfy.
